# rotating_square
A small terminal animation engine written in C that smoothly rotates a 2D square directly in the terminal without flickering. It combines simple geometry and terminal rendering techniques to draw animated shapes using basic characters like `#`.

Compile and run
- Don't forget to add -lm in linux to include the math.h liberary

```bash
gcc square.c -o square -lm
./square
```
